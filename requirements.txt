# Make sure pip can pull prebuilt wheels
pip>=24.0
setuptools>=65.0
wheel

# Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0

# Utilities
python-multipart==0.0.6
python-dotenv==1.0.0

# Core dependencies
# Upgrade pydantic to a version that ships wheels
pydantic==2.5.3

# This package is NOT official — consider replacing with Flask-CORS or using middleware
fastapi-cors==0.0.6
