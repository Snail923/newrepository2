# Ensure prebuilt wheels are used
pip>=25.1
setuptools>=65.0
wheel

# Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0

# Utilities
python-multipart==0.0.6
python-dotenv==1.0.0

# Pydantic (must have wheels to avoid Rust build)
pydantic==2.6.1
